# OpenProject Docker Configuration
# Copy this file to .env and modify as needed

# Database Configuration
POSTGRES_PASSWORD=p4ssw0rd
DATABASE_URL=postgres://postgres:p4ssw0rd@db/openproject?pool=20&encoding=unicode&reconnect=true

# OpenProject Configuration
OPENPROJECT_HOST__NAME=localhost:8080
OPENPROJECT_HTTPS=false
OPENPROJECT_HSTS=false
PORT=8080

# Performance Tuning
RAILS_MIN_THREADS=4
RAILS_MAX_THREADS=16

# Email Configuration (optional)
IMAP_ENABLED=false

# Data Volume Paths
PGDATA=pgdata
OPDATA=opdata

# OpenProject Version (optional)
TAG=15-slim

# Relative URL Root (optional)
# OPENPROJECT_RAILS__RELATIVE__URL__ROOT=/openproject