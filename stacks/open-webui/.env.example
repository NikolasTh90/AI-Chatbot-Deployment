# Docker image tag for Open WebUI
# Use 'main' for latest development version or a specific version tag like 'v0.1.120'
WEBUI_DOCKER_TAG=main

# Note: The secret key is hardcoded in docker-compose.yml as '1234' as requested
# For production use, consider generating a secure random key instead